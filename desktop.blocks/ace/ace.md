**Блок предоставляет класс `ace` с минимальными настройками**

**Элементы:**
 * editor - елемент загружает `ace.js` с помощью loader'a

**Модификаторы:**
 * mod - режим редактора
 * theme - тема редактора

> Каждый из модификаторов загружает тот или иной .js файл необходимый классу.
